version: '3.8'

services:
  frontend:
    build:
      context: ./admin-panel
      dockerfile: Dockerfile
    ports:
      - "80:80"
    networks:
      - my-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    networks:
      - my-network

networks:
  my-network:
    driver: bridge 